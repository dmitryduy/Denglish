const initialState = {
    levelName: 'atlantic',
    currentPoints: 400,
    totalPoints: 500,
}

const profileReducer = (state=initialState, action) => {
return state;
}

export default profileReducer;